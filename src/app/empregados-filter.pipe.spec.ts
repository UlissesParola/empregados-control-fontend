import { EmpregadosFilterPipe } from './empregados-filter.pipe';

describe('EmpregadosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpregadosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
